apis:
  realtime_quotes:
    allow_fallback: true
    fallback_order:
      - pytdx
      - akshare_em_spot
      - easyquotation_sina
      - easyquotation_tencent

  full_realtime_quotes:
    allow_fallback: true
    fallback_order:
      - pytdx
      - akshare_em_spot
      - easyquotation_sina
      - easyquotation_tencent

  index_realtime:
    allow_fallback: true
    fallback_order:
      - pytdx
      - easyquotation_sina
      - easyquotation_tencent

  minute_kline:
    allow_fallback: false
    fallback_order:
      - pytdx
    supported_periods:
      - 1m
      - 5m
      - 15m
      - 30m
      - 60m

  daily_kline:
    allow_fallback: false
    fallback_order:
      - pytdx
    supported_periods:
      - 1d

  kline:
    allow_fallback: false
    fallback_order:
      - pytdx
    supported_periods:
      - 1m
      - 5m
      - 15m
      - 30m
      - 60m
      - 1d
